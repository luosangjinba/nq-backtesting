# TF切换保持视图测试

## 功能说明
切换时间周期（TF）时，保持当前显示的时间区间不变，只改变K线的聚合粒度。

## 实现逻辑

### 1. 保存当前时间范围
在 `tfSelect.onchange` 事件中：
- 检查是否处于回放模式且已显示多根K线
- 保存当前显示的第一根和最后一根K线的时间戳到 `replayState.savedTimeRange`

### 2. 恢复时间范围
在 `loadData()` 函数中：
- 检查是否存在 `savedTimeRange`
- 如果存在，在新TF的数据中找到对应的结束时间索引
- 设置 `replayState.currentIndex` 为该索引
- 显示从第一根到该索引的所有K线

### 3. 保持视图位置
在 `onDataReady` 处理器中：
- 如果 `initialIndex > 0`（表示是TF切换），跳过 `resetView()`
- 这样视图位置不会被重置到最右边

## 测试步骤

1. 打开浏览器访问 http://127.0.0.1:8000/v2/docs/kline_viewer.html

2. 加载数据：
   - 开始时间：2024-01-02 09:00
   - 结束时间：2024-01-02 16:00
   - TF：1 分钟
   - 点击"加载数据"

3. 前进回放：
   - 点击"前进"按钮多次，或拖动滑块，显示约100-200根K线

4. 切换TF：
   - 将TF从"1分钟"切换到"5分钟"
   - **预期结果**：显示的时间范围保持不变，只是K线变粗（5分钟聚合）
   - **之前的问题**：会重置到只显示第一根K线

5. 再次切换TF：
   - 切换到"15分钟"
   - **预期结果**：时间范围继续保持，K线进一步聚合

6. 切换回小周期：
   - 切换回"1分钟"
   - **预期结果**：时间范围保持，K线变细

## 技术细节

### 修改的文件
- `v2/docs/kline_viewer.html`

### 修改的代码段
1. `replayState` 对象：添加 `savedTimeRange` 字段
2. `tfSelect.onchange` 事件处理器：保存时间范围
3. `loadData()` 函数：恢复时间范围逻辑
4. `onDataReady` 处理器：条件性跳过 `resetView()`

### 关键变量
- `replayState.savedTimeRange`: `{ startTime, endTime }` 或 `null`
- `initialIndex`: 恢复后的起始索引（0表示首次加载）

## 注意事项

1. **时间对齐**：新TF的K线时间戳可能不完全匹配旧TF的结束时间，代码会找到最接近的K线
2. **边界情况**：如果新TF的数据不足以覆盖原时间范围，会显示到最后一根可用K线
3. **视图锚点**：切换TF后 `viewportAnchor` 会被重置为 `null`，用户需要重新滚动来设置锚点

## 已知限制

- 切换TF后，Y轴压缩状态会保持（`yAxisCompressed` 标志不会重置）
- 如果用户在回放过程中暂停并切换TF，播放状态会保持暂停
