import { VALIDATION_SUMMARY_COMMANDS,VALIDATION_SUMMARY_EVENTS } from '../contracts/app-contracts.js';import{registerCommand}from'../runtime/commands.js';
export function createCampaignSummaryRuntime({repository}={}){if(!repository)throw new Error('Campaign Summary runtime requires repository.');const un=[];return Object.freeze({id:'runtime.validation-campaign-summary',async start({emitEvent}={}){await repository.open?.();un.push(registerCommand(VALIDATION_SUMMARY_COMMANDS.GET,({campaignId}={})=>repository.project(campaignId)),registerCommand(VALIDATION_SUMMARY_COMMANDS.DRILLBACK,async payload=>{try{const descriptor=await repository.drillback(payload);const result={descriptor,status:'resolved'};emitEvent?.(VALIDATION_SUMMARY_EVENTS.DRILLBACK_RESOLVED,result);return result;}catch(error){const result={reason:error?.message||String(error),status:'rejected'};emitEvent?.(VALIDATION_SUMMARY_EVENTS.DRILLBACK_REJECTED,result);return result;}}));},async stop(){while(un.length)un.pop()();await repository.close?.();}});}
