# Session 2026-05-19: V4 重构启动

## 目标
从零构建 v4，独立运行，小步慢跑。

## 关键决策
- 所有 PDA 均为前端实时计算，不存数据库
- v4 不需要 v2_research.duckdb，只需 trading_data.duckdb
- v4 专属 API 服务器（端口 8766）
- 使用 LightweightCharts v5.2.0（从 v4 升级）
- 手动标注存前端状态，不持久化

## 完成内容
- Step 1-7: 目录骨架、事件总线、配置、图表管理器、图元、API+后端、数据存储+工具栏、数据绑定、周期切换
- v4_api.py 从 price_lookup_api.py 导入查询函数（不复制代码）
- K 线加载和周期切换已验证通过

## 发现
- LightweightCharts v5 内置插件：Markers、Watermark、UpDownMarkers
- BSL/SSL 标签可用 createSeriesMarkers() 替代自定义 Primitive
- v5 的 addSeries(CandlestickSeries, opts) 替代 v4 的 addCandlestickSeries(opts)
- 系统 Python 无 duckdb，需用 /home/leo/miniconda3/bin/python3

## 待实现
- Step 8: PDA 类型注册表
- Step 9: PDA 扫描器（前端实时计算）
- Step 10: PDA 渲染器 + 存储
- Step 11: 逐根播放
- Step 12: 右键菜单 + PDA 详情 + 手动标注
- Step 13: 键盘快捷键 + CSS 整合

## 补充完成
- 日线聚合修复：tf=1440 使用 22:00 UTC (6:00 PM ET) 作为 CME 交易日分界，替代 query_v2_bars 默认的 00:00 UTC
  - v4_api.py 新增 query_v4_bars()，仅对 tf=1440 覆盖，其他 tf 仍走 query_v2_bars
  - 验证：DuckDB 直接聚合与 API 返回的日线 OHLCV 完全一致
- OHLCV 悬停 legend：鼠标悬停 K 线时，图表左上角显示 O/H/L/C 数值，涨绿跌红
  - 使用 subscribeCrosshairMove 事件 + #ohlc-legend overlay
- 数据验证：2012-01-11 10:02 bar O=2355.25 C=2357.25，与用户端一致

## 文件清单
```
v4/
  v4_config.yaml, v4_api.py, start.sh, index.html, style.css
  src/app.js, event-bus.js, config.js, api.js
  src/chart/chart-manager.js, primitives.js
  src/data/bar-store.js
  src/ui/toolbar.js
```
