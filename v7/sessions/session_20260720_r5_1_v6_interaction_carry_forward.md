# V7 R5.1 V6 Interaction Decision Carry-Forward — 2026-07-20

## Decision

Treat clear, non-rejected V6 interaction decisions as prior product evidence.
V7 will not ask the user to repeat settled interaction design for Reset View,
Replay transport, Settings, multi-pane, ETH/RTH, or multi-instrument behavior.

## Boundary

The audit retains product semantics and rejects V6 runtime ownership. It adds
no production code, UI, runtime, persistence, chart mutation, provider call, or
calendar assumption. Every future implementation still receives a V7 owner,
public contract, atomic transaction path, stale-result gate, and harness.

## Outcome

The binding carry-forward matrix records inherited behavior, V7 treatment,
reopen triggers, source evidence, and rejected patterns. R5.2 may begin with
actual timestamp/calendar verification and the pure ETH/RTH domain without
repeating the V6 product-definition phase.

## Automated Evidence

- architecture hardening and boundary harnesses;
- required-document registration;
- current-step ordering metadata;
- `git diff --check`.

This documentation-only step changes no browser interaction or visuals, so its
acceptance is based on automated evidence under the standing workflow.
